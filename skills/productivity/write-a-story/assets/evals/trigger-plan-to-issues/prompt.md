---
max_turns: 10
allowed_tools: [Skill, Read, Glob, Grep]
---

Turn this plan into tickets.

Plan: add SAML SSO for enterprise accounts. Phase 1, build the SAML handler and the metadata endpoint. Phase 2, an admin UI to upload IdP metadata and test the connection. Phase 3, enforce SSO-only login per workspace. Okta and Azure AD only.
